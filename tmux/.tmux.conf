set -g default-terminal "screen-256color"
set -g mouse on
set -s set-clipboard on
set -g allow-passthrough
set-option -g status-interval 1
set-option -g automatic-rename on
set-option -g automatic-rename-format '#{b:pane_current_path}'
bind-key -n S-Left swap-window -t -1
bind-key -n S-Right swap-window -t +1
set-window-option -g mode-keys vi
set -g history-limit 10000
set -g base-index 1
setw -g pane-base-index 1

bind | split-window -h -c "#{pane_current_path}"
bind-key -Tcopy-mode-vi 'v' send -X begin-selection
bind-key -Tcopy-mode-vi 'y' send -X copy-selection
bind P paste-buffer
set -g status-interval 2

# window status
setw -g window-status-format " #F#I:#W#F "
setw -g window-status-current-format " #F#I:#W#F "
#setw -g window-status-format "#[fg=magenta]#[bg=black] #I #[bg=cyan]#[fg=colour8] #W "
#setw -g window-status-current-format "#[bg=brightmagenta]#[fg=colour8] #I #[fg=colour8]#[bg=colour14] #W "
#setw -g window-status-current-style bg=colour0,fg=colour11,dim
set -g status-left ''

set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence off
set-window-option -g monitor-activity off
set-option -g bell-action none
bind Tab last-window

#setw -g clock-mode-colour colour135
#setw -g mode-style fg=colour192,bg=colour238,bold

#set -g pane-border-style bg=colour1,fg=colour122
#set -g pane-active-border-style bg=colour236,fg=colour51

# }
# The statusbar {

#set -g status-position bottom
#set -g status-style bg=colour234,fg=colour137,dim
#set -g status-left ''
#set -g status-right '#[fg=colour233,bg=colour241,bold] [w: c=create, &=kill; p: x=kill] #[fg=colour233,bg=colour245,bold] Ctrl+e'
#set -g status-right-length 50
#set -g status-left-length 20

#setw -g window-status-current-style fg=colour81,bg=colour238,bold
#setw -g window-status-current-format ' #I#[fg=colour250]:#[fg=colour255]#W#[fg=colour50]#F '
#
#setw -g window-status-style fg=colour138,bg=colour235,none
#setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '
#
#setw -g window-status-bell-style fg=colour255,bg=colour1,bold
#

#set -g message-style fg=colour232,bg=colour166,bold



set -g @plugin 'tmux-plugins/tmux-prefix-highlight'

set -g @plugin 'jimeh/tmux-themepack'
set -g @themepack 'powerline/double/magenta'
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'omerxx/tmux-sessionx'
run '~/.tmux/plugins/tpm/tpm'
#bind-key f run-shell '~/.tmux_q_menu.sh'
set -g pane-border-status top
set -g pane-border-format "#{session_name}:#{window_index}.#{pane_index}"
set -g status-left "<#{session_name}> |
